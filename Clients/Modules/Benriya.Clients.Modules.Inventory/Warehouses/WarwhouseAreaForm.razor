@using Benriya.Clients.Wasm.Components.Grid
@using Benriya.Clients.Wasm.Components.Modals
@using Benriya.Clients.Wasm.Components.Forms
@using Benriya.Modules.Inventory.Share.Models.Warehouses
@inherits Benriya.Clients.Modules.Inventory.Warehouses.WarwhouseAreaFormBase

@*<ModalContent @ref="modalForm">
    <Title>This is a <em>Title!</em> - @ModelData.id</Title>
    <Content>*@
        <EditForm Model="@ModelData" OnValidSubmit="@HandleValidSubmitModal">
            <DataAnnotationsValidator />
            <FormServValidator @ref="formValidator"/>
            <div class="modal-body">
                <div class="row">
                    <div class="form-group col-4">
                        <label>Name:</label>
                        <InputText @bind-Value="ModelData.name" class="form-control form-control-sm" type="text" />
                        <ValidationMessage For="@(() => ModelData.name)" />
                    </div>
                    <div class="form-group col-4">
                        <label>Desciption</label>
                        <InputText @bind-Value="@ModelData.description" class="form-control form-control-sm" type="text" />
                        <ValidationMessage For="@(() => ModelData.description)" />
                    </div>
                </div>
            </div>
            <div class="modal-footer justify-content-center">

                <button type="submit" class="btn btn-primary">Save</button>
                @if (isNew)
                {
                    <button type="button" class="btn btn-secondary" @onclick="@OnResetModel">Clear</button>                    
                }
                else
                {
                    <button type="button" class="btn btn-secondary" @onclick="@OnCancelModel">Close</button>
                }

            </div>
        </EditForm>
    @*</Content>
</ModalContent>*@